# ── server.py dependencies ──────────────────
flask>=3.1.3          # API server framework

# ── client.py dependencies ──────────────────
mcp[cli]>=1.27.0      # MCP server (FastMCP) — [cli] includes uvicorn runner
requests>=2.33.1      # HTTP client to talk to the Flask API server
